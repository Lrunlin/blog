const $color = "rgba(14, 253, 255, 1)";
const $borderColor = "rgba(14, 253, 255, 0.5)";
const vw = ($px: number) => `${($px * 100) / 1920}vw`;
const vh = ($px: number) => `${($px * 100) / 961}vh`;
export { $color, $borderColor, vw, vh };
